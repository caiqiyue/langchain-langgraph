# -*- coding: utf-8 -*-
"""
【案例 2】安全优先原则的深度实施
=================================

本案例深入讲解如何在中间件编排中实施安全优先原则，
包括分层安全检查和冲突解决机制。

分层安全检查体系：
1. 身份认证（最外层）- 确保合法用户
2. 输入验证（第二层）- 检查恶意代码
3. 业务授权（第三层）- 检查操作权限
4. 合规检查（最内层）- 符合法规要求

冲突解决机制：
1. 资源冲突 - 锁机制 vs 队列管理
2. 结果冲突 - 权重决策 vs 投票机制

要点：
1. 理解分层安全检查的设计思想
2. 掌握四种安全检查类型的实施
3. 理解冲突解决机制
"""

# ============================================================
# 1. 环境配置
# ============================================================
import os
from dotenv import load_dotenv

load_dotenv(override=True)

print("=" * 60)
print("案例 2: 安全优先原则的深度实施")
print("=" * 60)

# ============================================================
# 2. 分层安全检查体系
# ============================================================
print("\n【分层安全检查体系】")
print("-" * 50)

print("""
┌─────────────────────────────────────────────────────────┐
│                     分层安全检查                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  第1层：身份认证（before_agent 最外层）                 │
│  ┌─────────────────────────────────────────────────┐   │
│  │  检查用户是否登录、Token 是否有效                │   │
│  │  → 未认证用户直接拒绝                           │   │
│  └─────────────────────────────────────────────────┘   │
│                         ↓                               │
│  第2层：输入验证（before_agent）                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │  检查输入内容是否包含恶意代码、危险关键词        │   │
│  │  → 危险输入直接拒绝                             │   │
│  └─────────────────────────────────────────────────┘   │
│                         ↓                               │
│  第3层：业务授权（before_agent / before_model）         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  检查用户是否有权执行操作（RBAC）               │   │
│  │  → 无权限操作直接拒绝                           │   │
│  └─────────────────────────────────────────────────┘   │
│                         ↓                               │
│  第4层：合规检查（wrap_model_call）                     │
│  ┌─────────────────────────────────────────────────┐   │
│  │  检查操作是否符合法规要求（PII、敏感词）         │   │
│  │  → 不合规操作标记或拒绝                         │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
""")

# ============================================================
# 3. 各层实现示例
# ============================================================
print("\n【各层实现示例】")
print("-" * 50)

print("""
【第1层：身份认证】
class AuthenticationMiddleware(AgentMiddleware):
    def before_agent(self, state, runtime):
        token = runtime.context.get("token")
        if not validate_token(token):
            return {"jump_to": "end", "error": "未认证"}
        return None

【第2层：输入验证】
class InputValidationMiddleware(AgentMiddleware):
    def before_agent(self, state, runtime):
        dangerous_keywords = ["删除数据库", "hack", "攻击"]
        last_msg = state["messages"][-1].content
        for kw in dangerous_keywords:
            if kw in last_msg:
                return {"jump_to": "end", "error": "危险操作"}

【第3层：业务授权】
class RBACMiddleware(AgentMiddleware):
    def before_agent(self, state, runtime):
        user_role = runtime.context.get("role")
        if not has_permission(user_role, operation):
            return {"jump_to": "end", "error": "权限不足"}

【第4层：合规检查】
class ComplianceMiddleware(AgentMiddleware):
    def before_model(self, state, runtime):
        if contains_pii(state["messages"]):
            return {"messages": sanitize(messages)}
""")

# ============================================================
# 4. 冲突解决机制
# ============================================================
print("\n【冲突解决机制】")
print("-" * 50)

print("""
┌─────────────────────────────────────────────────────────┐
│                     冲突类型与解决                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  【资源冲突】                                           │
│  问题：多个中间件同时访问资源                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 方案1：锁机制                                    │   │
│  │  - 保证资源独占访问                               │   │
│  │  - 缺点：降低并发性能                            │   │
│  │                                                  │   │
│  │ 方案2：队列管理                                   │   │
│  │  - 提供公平的资源分配                            │   │
│  │  - 缺点：增加延迟                                │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  【结果冲突】                                           │
│  问题：多个中间件产生不同结果                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 方案1：权重决策                                   │   │
│  │  - 根据预定义规则选择结果                        │   │
│  │                                                  │   │
│  │ 方案2：投票机制                                   │   │
│  │  - 多个中间件投票决定                            │   │
│  │                                                  │   │
│  │ 方案3：最终决策者                                 │   │
│  │  - 指定某个中间件为最终决策者                    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
""")

# ============================================================
# 5. 依赖关系管理
# ============================================================
print("\n" + "=" * 60)
print("依赖关系管理")
print("=" * 60)

print("""
【依赖类型分析】

1. 顺序依赖
   A → B（A 必须在 B 之前执行）
   例：身份认证 → 权限验证

2. 条件依赖
   A? → B 或 C（根据条件决定后续）
   例：VIP用户 → 高级处理，普通用户 → 标准处理

3. 数据依赖
   A 产生数据 → B 使用数据
   例：RBACMiddleware 注入权限 → dynamic_prompt 使用权限

【依赖排序策略】

无依赖 → 首先执行
  ↓
单向依赖 → 按依赖关系排序
  ↓
条件依赖 → 特殊处理逻辑
  ↓
聚合依赖 → 等待所有相关完成
""")

print("\n" + "=" * 60)
print("案例结束")
print("=" * 60)