# -*- coding: utf-8 -*-
"""
【案例 1】中间件编排黄金法则 - 执行顺序设计
============================================

本案例讲解中间件编排的核心原则，帮助设计高效、安全的中间件组合。

四大黄金法则：
1. 安全类永远优先（安全 > 业务 > 成本 > 监控）
2. 写操作按依赖排序（A 在 B 之前，则 A 先执行）
3. 中断操作置后（同类型中间件的最后执行）
4. 性能开销降序排列（轻量级 → 重量级）

执行顺序表：
| 钩子类型          | 执行顺序 | 最佳实践               |
|-----------------|--------|----------------------|
| before_agent    | 正序    | 安全/初始化优先        |
| before_model    | 正序    | 预算/合规前置          |
| wrap_model_call | 嵌套    | 路由/重试/缓存核心     |
| after_model     | 逆序    | 验证/日志后置          |
| wrap_tool_call  | 嵌套    | 参数校验/重试保护      |

要点：
1. 理解四大黄金法则
2. 掌握执行顺序设计方法
3. 理解分层治理的思想
"""

# ============================================================
# 1. 环境配置
# ============================================================
import os
from dotenv import load_dotenv

load_dotenv(override=True)

print("=" * 60)
print("案例 1: 中间件编排黄金法则")
print("=" * 60)

# ============================================================
# 2. 洋葱模型图示
# ============================================================
print("\n【中间件洋葱模型】")
print("-" * 50)

print("""
        ┌────────────────────────────────────────┐
        │           Agent 执行核心               │
        │  ┌──────────────────────────────────┐  │
        │  │       wrap_tool_call            │  │
        │  │    ┌────────────────────────┐   │  │
        │  │    │   wrap_model_call     │   │  │
        │  │    │  ┌─────────────────┐  │   │  │
        │  │    │  │ before_model   │  │   │  │
        │  │    │  │  ┌─────────┐   │  │   │  │
        │  │    │  │  │before_  │   │  │   │  │
        │  │    │  │  │agent    │   │  │   │  │
        │  │    │  │  └─────────┘   │  │   │  │
        │  │    │  └─────────────────┘  │   │  │
        │  │    └────────────────────────┘   │  │
        │  └────────────────────────────────┘  │
        └────────────────────────────────────────┘

分层职责：
  外层 → 安全防护（Security, RBAC）
  中层 → 业务逻辑（Routing, Transform）
  内层 → 核心功能（Model Call, Tool Call）
  中心 → 基础服务（LLM, Tools）
""")

# ============================================================
# 3. 四大黄金法则
# ============================================================
print("\n【四大黄金法则】")
print("-" * 50)

print("""
法则一：安全类永远优先
┌─────────────────────────────────────────┐
│ 优先级：安全 > 业务 > 成本 > 监控        │
│                                         │
│ 示例：                                   │
│   SecurityGuardrail → RBAC → Business → │
│   → Cost → Monitor                      │
└─────────────────────────────────────────┘

法则二：写操作按依赖排序
┌─────────────────────────────────────────┐
│ 如果 B 依赖 A 的输出结果，则 A 先执行    │
│                                         │
│ 示例：                                   │
│   RBACMiddleware(注入用户信息) →         │
│   dynamic_prompt(使用用户角色)           │
└─────────────────────────────────────────┘

法则三：中断操作置后
┌─────────────────────────────────────────┐
│ 中断类中间件放在同类型最后执行            │
│                                         │
│ 示例：                                   │
│   before_agent 顺序：                    │
│     [Security, RBAC, ..., Interrupt?]   │
│                         ↑               │
│                    中断操作放最后        │
└─────────────────────────────────────────┘

法则四：性能开销降序排列
┌─────────────────────────────────────────┐
│ 轻量级 → 重量级（快速失败）               │
│                                         │
│ 性能开销排序：                           │
│   Monitor(<1ms) → Enforce(1-5ms) →      │
│   Control(5-20ms) → Modify(1-50ms)      │
└─────────────────────────────────────────┘
""")

# ============================================================
# 4. 执行顺序表
# ============================================================
print("\n【执行顺序设计表】")
print("-" * 50)

顺序表 = """
| 钩子类型          | 执行顺序 | 控制粒度   | 推荐实践               |
|-----------------|--------|----------|----------------------|
| before_agent    | 正序    | 调用级    | 安全检查、初始化       |
| before_model    | 正序    | 单次调用  | 输入验证、预算控制     |
| wrap_model_call | 嵌套    | 调用全程  | 路由、重试、缓存       |
| after_model     | 逆序    | 单次调用  | 验证、日志             |
| wrap_tool_call  | 嵌套    | 工具执行  | 参数校验、重试保护      |
| after_agent     | 逆序    | 全局      | 清理、持久化           |

注意：
  - before_* 和 wrap_* 正序执行
  - after_* 逆序执行（最后注册的先执行）
  - wrap_* 嵌套执行（形成包裹结构）
"""
print(顺序表)

# ============================================================
# 5. 典型组合示例
# ============================================================
print("\n" + "=" * 60)
print("典型组合示例")
print("=" * 60)

print("""
【IT 运维 Agent 中间件组合】

middlewares = [
    # === before_agent（正序）===
    SecurityGuardrail(),        # 1. 安全检查
    RBACMiddleware(),           # 2. 权限验证

    # === before_model（正序）===
    SummarizationMiddleware(),  # 3. 上下文压缩

    # === wrap_model_call（嵌套）===
    @dynamic_prompt,            # 4. 动态提示词
    @model_router,              # 5. 模型切换

    # === after_model（逆序）===
    AuditLogger(),             # 6. 审计日志（后注册先执行）
    ResponseValidator(),        # 5. 响应验证
]

设计理由：
  1. Security 最先 - 危险操作拦截
  2. RBAC 其次 - 权限验证
  3. Summarization 在前 - 压缩上下文
  4. dynamic_prompt 和 model_router 嵌套执行
  5. after_model 逆序 - AuditLogger 最后执行
""")

print("\n" + "=" * 60)
print("案例结束")
print("=" * 60)