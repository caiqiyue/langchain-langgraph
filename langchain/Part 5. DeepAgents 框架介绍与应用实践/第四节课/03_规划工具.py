# -*- coding: utf-8 -*-
"""
【案例 3】规划工具 (TodoList)
==============================

本案例展示 DeepAgents 中规划工具的作用和用法。

规划工具组件：TodoListMiddleware

核心工具：write_todos
- 在行动前生成待办事项列表
- 将任务分解为可执行的子步骤
- 在执行过程中更新状态

工作流：
1. Agent 接收复杂任务
2. 调用 write_todos 分解任务（Pending）
3. 每完成一步，更新状态（Completed）
4. 自我反思，避免迷失方向

要点：
1. 理解规划工具的作用
2. 掌握 write_todos 的用法
3. 理解状态管理机制
"""

# ============================================================
# 1. 环境配置
# ============================================================
import os
from dotenv import load_dotenv

load_dotenv(override=True)

print("=" * 60)
print("案例 3: 规划工具 (TodoList)")
print("=" * 60)

# ============================================================
# 2. 规划工具概述
# ============================================================
print("\n【规划工具概述】")
print("-" * 50)

print("""
组件：TodoListMiddleware

工具：write_todos

功能：
  - Agent 在行动前生成 Markdown 格式的待办事项列表
  - 在执行过程中更新状态（完成/进行中）
  - 强制模型进行"思维链"的显性化管理

触发条件：
  - 简单短期任务不会触发
  - 复杂任务自动触发
""")

# ============================================================
# 3. 工作流程图
# ============================================================
print("\n【工作流程】")
print("-" * 50)

流程图 = """
┌─────────────────────────────────────────────────────────┐
│                     规划工具工作流                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   1. 接收复杂任务                                        │
│          ↓                                              │
│   2. write_todos 分解为子步骤 (Pending)                │
│          ↓                                              │
│   3. 执行第一步                                         │
│          ↓                                              │
│   4. 更新状态为 (Completed)                             │
│          ↓                                              │
│   5. 继续执行下一步                                     │
│          ↓                                              │
│   6. 所有步骤完成 → 生成回答                            │
│                                                         │
└─────────────────────────────────────────────────────────┘
"""
print(流程图)

# ============================================================
# 4. Todo 状态字段
# ============================================================
print("\n【Todo 状态字段】")
print("-" * 50)

字段表 = """
| 字段          | 类型     | 说明                    |
|---------------|----------|-------------------------|
| id           | str      | 唯一标识                 |
| description   | str      | 任务描述                 |
| status       | str      | pending/completed       |
| priority     | int      | 优先级（可选）          |
| dependencies  | list[str]| 依赖关系（可选）        |
"""
print(字段表)

# ============================================================
# 5. 使用示例
# ============================================================
print("\n【使用示例】")
print("-" * 50)

print("""
待办事项 JSON 示例：

{
  "todos": [
    {
      "id": "1",
      "description": "搜索相关信息",
      "status": "completed",
      "priority": 1
    },
    {
      "id": "2",
      "description": "整理资料",
      "status": "pending",
      "priority": 2
    },
    {
      "id": "3",
      "description": "撰写报告",
      "status": "pending",
      "priority": 3
    }
  ]
}

Agent 会在每一步行动前看到当前的 Todo List，
从而避免迷失方向。
""")

print("\n" + "=" * 60)
print("案例结束")
print("=" * 60)