# -*- coding: utf-8 -*-
"""
【案例 1】DeepAgents 框架定位
=================================

本案例展示 DeepAgents 在 LangChain 生态系统中的定位。

框架层级：
- LangChain：基础组件库（积木块）
- LangGraph：编排引擎（图结构）
- DeepAgents：应用框架（最佳实践）

要点：
1. 理解三大框架的层级关系
2. 掌握 DeepAgents 的核心价值
3. 理解为何 DeepAgents 能加速开发
"""

# ============================================================
# 1. 环境配置
# ============================================================
import os
from dotenv import load_dotenv

load_dotenv(override=True)

print("=" * 60)
print("案例 1: DeepAgents 框架定位")
print("=" * 60)

# ============================================================
# 2. 框架层级对比
# ============================================================
print("\n【框架层级对比】")
print("-" * 50)

print("""
┌─────────────────────────────────────────────────────────┐
│                    DeepAgents                            │
│              (应用框架 / Application Framework)          │
│         提供开箱即用的组件和高级 API                     │
├─────────────────────────────────────────────────────────┤
│                     LangGraph                            │
│                (编排引擎 / Orchestration)                │
│           提供 StateGraph、Node、Edge                    │
├─────────────────────────────────────────────────────────┤
│                    LangChain                             │
│              (基础组件库 / Foundation)                   │
│            提供 Prompt、Models、Tools                    │
└─────────────────────────────────────────────────────────┘
""")

# ============================================================
# 3. 核心特性对比表
# ============================================================
print("\n【核心特性对比】")
print("-" * 50)

对比表 = """
| 特性       | LangChain      | LangGraph      | DeepAgents           |
|------------|----------------|----------------|---------------------|
| 层级       | 基础组件库     | 编排引擎       | 应用框架            |
| 核心抽象   | Chain, Runnable| StateGraph     | DeepAgent, Middleware|
| 灵活性     | 极高（积木块） | 高（图结构）   | 中（约定优于配置）  |
| 开箱即用   | 低（需组装）   | 中（需定义图） | 高（内置组件）      |
| 适用对象   | 库开发者       | 复杂流程序列化  | 应用开发者/企业    |
"""
print(对比表)

# ============================================================
# 4. DeepAgents 本质
# ============================================================
print("\n【DeepAgents 本质】")
print("-" * 50)

print("""
DeepAgents 是 LangGraph 的"最佳实践实现"：

1. 底层使用 LangGraph 管理状态和循环
2. 向上提供更高级的 API（create_deep_agent）
3. 隐藏了底层的图构建细节
4. 内置规划、文件系统、子代理等组件

对比：
  - LangChain：需要自己组装积木
  - LangGraph：需要自己定义图逻辑
  - DeepAgents：开箱即用，专注业务逻辑
""")

# ============================================================
# 5. 选择建议
# ============================================================
print("\n【选择建议】")
print("-" * 50)

选择表 = """
| 场景                    | 推荐框架     |
|------------------------|-------------|
| 需要自定义提示与工具     | LangChain    |
| 构建复杂多智能体系统     | LangGraph    |
| 快速实现 AutoGPT 类应用 | DeepAgents   |
| 企业级解决方案          | DeepAgents   |
"""
print(选择表)

print("\n" + "=" * 60)
print("案例结束")
print("=" * 60)